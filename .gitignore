# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 私钥 / 环境。dapp 里用户私钥不会进代码（只拿签名能力），
# 但 builder 收款地址、测试用私钥这类以后可能塞进 .env，提前挡住。
.env
.env.*
!.env.example

# Claude Code 的 worktree 是本地工作副本，不该进版本库。
# 只挡 worktrees —— settings.json 之类的项目配置仍可提交。
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
