# 项目目标

这个文件是**活的**:目标变了、某条做完了、发现某条其实做不到 —— 就地改这里,
而不是让它在对话里过期。最后更新:2026-09-22。

## 一句话

一个**纯前端**的 Polymarket 足球盘口 dapp:把一场比赛的全部盘口画成一张关系图
(网状盘口图),节点上直接下单。没有服务器、没有数据库。

## 四条目标

1. 极致简洁、新颖、易操作的用户前端(网状盘口图)
2. 纯前端项目
3. 流畅的用户体验(加载 + 响应两方面)
4. 订单分析统计

---

## 目标一:网状盘口图

**现状 —— 已成立**

- 关系图由 Gamma 的盘口列表现算(`src/graph/graph.ts` 的 `buildMarketGraph`),
  一场比赛实测 53 节点 / 62 边、85 个盘口
- 画布按容器比例排布,点节点 → 下单面板(`src/components/market-graph-canvas.tsx`)
- 结构关系与实时价格叠在一层:结构来自 Gamma(不随价格变),价格来自 CLOB
  WebSocket;两者在界面上区分「实时双边盘」与「Gamma 快照」
- 槽位模板把图上的抽象节点解析成可点的盘口(`src/graph/template.ts`)

**未决 —— 缺一个可验收的标准**

「极致简洁新颖易操作」目前是三个形容词,没人能据此说「这版比上版好」。要往下走
得先把它拆成能判断的问题,比如:

- 节点的视觉层次:哪些是主盘口、哪些是衍生,现在靠什么区分?
- 边的语义:是「约束」还是「相关」,用户看得出来吗?
- 「找到我想下的那个盘口」要几次操作?现在要先选比赛 → 找节点 → 选侧 → 下单

**下一步:需要一个具体的不满意场景。** 没有场景就只能在猜,猜出来的多半不是
想改的那个。

---

## 目标二:纯前端

**现状 —— 成立,而且比原先设想的更彻底**

三条链路都在浏览器里完成:

| 环节 | 为什么可以纯前端 |
|---|---|
| 赛事/盘口数据 | Gamma 的 CORS 全开(`access-control-allow-origin: *`),公开只读 |
| 下单签名 | 用户钱包在浏览器里签(Deposit Wallet 走 ERC-7739 外壳,SDK 内部处理) |
| 交易授权 | 由 Polymarket 的 relayer 代发,用户不花 gas |
| 费率查询 | `fetchBuilderFeeRates` 走公共客户端,不弹签名 |
| 订单/持仓/盈亏历史 | 公共客户端按地址查,不弹签名(见目标四) |

**已作废的旧假设**

`src/lib/builder.ts` 的注释里订正过一件事:曾经以为「挂 builder code 需要后端签名
服务」,追过 SDK 实现后发现 `builder` 只是订单数据的一个字段,不需要 HMAC 认证头,
所以不需要后端。**Builder API key 是另一回事** —— 那个是真凭据,但全 SDK 只有两处
要求它(combo RFQ 和 session-key authorization),本项目两个都不用。

**真正需要后端的,只剩两件**

1. **combo RFQ / session key** —— 唯二要求 builder API key 的地方,而这个 key
   官方明确警告不能暴露给前端
2. **跨用户的聚合统计** —— 需要枚举所有用户地址,浏览器里做不了

两条都不在当前目标里。所以「纯前端」这条**不需要再妥协**。

---

## 目标三:流畅(加载 + 响应)

**现状 —— 未测量。下面全是读代码得到的嫌疑,不是结论。**

按怀疑度排序:

### 嫌疑一(最大):比赛列表的顺序翻页

`src/lib/gamma.ts:151` 的 `fetchSoccerEvents`

用 `tag_id=1`(**整个体育大类**)拉,然后在 `gamma.ts:180` 才
`events.filter(isSoccer)` 筛足球。注释里自己写了「前 100 个体育事件里只挑得出
十来场足球」,而 `maxPages = 20` —— 最坏情况是 **20 次串行的 HTTP 往返**
(`for` 循环里 `await`,一次一页),每页还带着该事件下所有 markets 的完整 payload,
最后在客户端丢掉绝大部分。

两个改法:

- **让 Gamma 在服务端就筛掉足球**(如果存在足球专用的 tag),能把 20 页砍到 1~2 页
- **并行翻页**:预取 offset 0/100/200…,遇到第一个不满页就停。不改接口也能快数倍

### 嫌疑二:默认选中的是最重的比赛

`src/App.tsx:92` 首次拿到列表自动选 `matches[0]`,而列表按盘口数排序
(`gamma.ts:295`),所以选中的是**盘口最多的那场**(典型 85 个盘口 / 170 个 token)。
于是首屏 100% 命中最坏情况:建图最重、WS 要订阅 170 个 token。

### 嫌疑三:WS 第一帧是全量 dump

`src/lib/clob-ws.ts:185` 用 `initial_dump: true` 一次订阅 170 个 token,第一帧就是
170 个盘口的完整深度。CLOB 的 market channel **不支持增量订阅**(`clob-ws.ts:179`
已记录),所以「一次全给」是被迫的 —— 能动的只有**先订哪些**:可以先订顶档需要的,
其余的分批。

### 嫌疑四:比赛列表完全没缓存

`src/lib/use-graph.ts:51` 的 `useSoccerMatches` 是个裸 `useEffect`,项目其它地方
都用 react-query(还配了 `staleTime`),只有它没有。每次刷新都重付上面那 20 页。
比赛列表的变化是「按赛程冒出来」,给 5~10 分钟 `staleTime` 完全安全。

### 响应侧(第二个方面)

WS flush 间隔 400ms(`clob-ws.ts:55`),每次 flush 跑一遍 `applyLivePrices` +
`resolveTemplate`(53 节点 / 62 边)。**这条没测过**,不确定是不是瓶颈。

如果「响应」指的是**拖动画布卡**,那要先 profile 画布的重绘,不在数据这一层。

**下一步:先测量再改。** 在四个点插 `performance.now()` 或直接看 Network 面板的
瀑布图。如果 `fetchSoccerEvents` 占了大头,修掉嫌疑一就够,其余三个可以先不动。

---

## 目标四:订单分析统计

**结论:能做,纯前端,而且不需要弹签名。**

关键事实:SDK 的公共客户端有一整套**按地址查**的历史接口,参数是 `user`,而
`user` 就是我们已经解析出来的代理钱包地址(`lookupProxyWallet`,一次 GET)。

| 想要什么 | 接口 | 鉴权 |
|---|---|---|
| 成交明细 | `listTrades({user})` | 无 |
| 活动流水 | `listActivity({user})` | 无 |
| 当前持仓 | `listPositions({user})` | 无 |
| 盈亏曲线 | `fetchUserPnl({user})` | 无 |
| 成交量 | `fetchUserVolume({user})` | 无 |
| 汇总 | `fetchUserStats({user})` | 无 |
| **平台侧归因成交** | `listBuilderTrades({builderCode})` | 无 |

最后一行有额外价值:**`listBuilderTrades` 返回的每条记录都带 `feeUsdc`** —— 这正好
能结掉 `src/lib/fee.ts` 里挂着的那条待办(「费率是不是按名义额算的,未实测」)。
跑通第一笔真单之后拿它对一下,对上就把那段「未实测」的注释删掉。

**需要后端的只剩:** 跨用户聚合,以及时间序列快照。原项目把 Gamma 数据落 MySQL
换来的回放 / 违约历史 / λ 曲线,dapp 直连只能拿到**当刻快照** —— 这部分可以自救:
定时把派生结果写 IndexedDB,自己攒本地历史。代价是只对「这个浏览器访问过的比赛」
有历史。

---

## 附:费率与归因(当前正在收尾的活)

Builder code 已挂上下单(三个分支:限价 / 市价买 / 市价卖),费率改为从
`fetchBuilderFeeRates` 读,不再只信本地常量。

**要点:**

- 费率**不在订单里**。`SignedOrder` 的字段只有 `builder`(32 字节 code),没有
  feeRate —— 交易所拿 code 去自己的库里查费率并按查到的扣。所以本地常量对**实际
  扣款毫无影响**,纯粹是显示用的
- 费率按 code 配,**与用户无关**,所以是一个长 `staleTime` 的单例查询,不是每单
  查一次,也不是登录时查一次
- 拿不到就退回 `BUILDER_FEE_RATES`(5 bps),**不挡下单** —— 费率只影响显示
- **产品取舍:费率只做提示,不做「读取中」态、不因查不到而禁用按钮。** 用户
  爱用不用,不该为了一个显示问题给他加一道关卡
- 设置页上的值与常量不一致时,控制台留一行(那说明常量这个副本过期了)
- `fetchBuilderFeeRates` 是**独立函数**,只能从 `@polymarket/client/actions`
  子路径引 —— 从包根引会拿到 `undefined`,运行时才炸

**未验证的:**

- `fee.ts` 说手续费按**成交名义额**算,这是按命名推的,**没有实测**。用
  `listBuilderTrades` 的 `feeUsdc` 核对
- SDK 文档提到 "before market **and** builder taker fees",即可能有两笔费。
  **Polymarket 自己的市场费是多少没查过** —— 如果不是 0,`feeBreakdown` 只算了
  builder 这一半,合计仍会低于真实扣款。同样等第一笔真单核对

---

## 待办

- [ ] 目标三:先测量,确定瓶颈在不在 `fetchSoccerEvents`
- [ ] 目标一:给出一个具体的不满意场景
- [ ] 费率:第一笔真单跑通后,用 `listBuilderTrades` 的 `feeUsdc` 核对公式
- [ ] 费率:确认 Polymarket 自己的市场费是否为 0
- [ ] 目标四:等有了真实成交数据,再做统计界面
